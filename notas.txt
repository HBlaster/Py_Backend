crear entorno virtual: 		python -m venv <nombre de carpeta de entorno virtual>
correr entorno virtual: 	.\venv\Scripts\Activate.ps1

Ejecutar servidor uvircorn:	uvicorn <nobre del archivo>:app --reload
ejecutar servidor usando flask:	python <nombre del archivo>

